# Docs

This directory is for public documentation assets that do not belong in the
repository root.

## Public Site

The current public documentation site source lives in `site/`.
That directory is designed to be publishable through GitHub Pages using the repository workflow files.
